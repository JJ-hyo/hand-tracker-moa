# Hand Tracker — 디자인 정리

## 이 서비스는

카메라 영상을 웹 화면에 띄우고, 그 위에 보이는 **손을 실시간으로 인식해서 관절과 제스처를 그려주는** 웹앱이에요.
PC 웹캠뿐 아니라 폰 카메라를 무선으로 연결해 쓸 수 있고, 최종적으로는 스마트 글라스의 1인칭 시점 영상을 받는 것이 목표예요.
설치 없이 브라우저만으로 동작하며, 영상 분석은 전부 사용자 기기 안에서 처리돼요.

---

## 화면 구성

앱은 **두 단계**로 이루어져 있어요. 연결하기 → 보기.

### ① 연결 화면 (PC)

앱을 열면 처음 보이는 화면. 영상 소스를 하나 고르는 곳이에요.

**상단**
- 제목: `Hand Tracker`
- 상태 한 줄: `모델 로딩 중…` → `모델 준비 완료 · 카메라를 시작하세요` (준비되면 뒷부분이 청록으로 강조)

**카드 두 장 (나란히, 좁은 화면에선 위아래)**

| 이 기기 카메라 | 폰 / 외부 기기 |
|---|---|
| 설명: "PC 웹캠으로 바로 시작해요." | 설명: "폰 카메라 영상을 받아서 트래킹해요. 글라스 연결도 이 자리에 들어와요." |
| 카메라 선택 드롭다운 | `폰 연결하기` 버튼 (보조 스타일) |
| `카메라 시작` 버튼 (주요 스타일) | 누르면 아래에 펼쳐짐 ↓ |
| | · 흰 배경 QR 코드 |
| | · 6자리 연결 코드 (크게, 자간 넓게) |
| | · 안내 문구 ("폰으로 QR을 찍거나 … 코드를 입력하세요") |
| | · "폰에서 접속할 주소" 입력칸 |
| | · 버튼 이름이 `코드 새로 만들기`로 바뀜 |

**하단**
- 최근 로그 4줄 (작은 고정폭 글씨, 오류는 빨강)

### ② 라이브 화면 (PC)

카메라가 연결되면 자동으로 넘어가요. **영상이 화면 전체를 채우고**, 그 위에 손 관절·제스처가 그려져요. 조작 요소는 상단 한 줄뿐이에요.

**상단 바** (영상 위에 얇게, 위쪽만 살짝 어둡게 처리)
- 왼쪽: `← 연결 해제` 버튼 · 소스 이름 (`PC 카메라` / `폰`) · `30 fps`
- 오른쪽:
  - 인식된 손 칩 — `L 🤏 Pinch` 처럼 손 방향 + 이모지 + 제스처 이름. 왼손은 청록, 오른손은 주황 테두리
  - 구분선
  - 토글 칩 3개: `반전` `관절` `궤적` (켜지면 청록)

**영상 위 오버레이**
- 관절 21개를 점과 선으로 (왼손 청록 / 오른손 주황, 점 테두리 흰색)
- 검지 끝 위에 제스처 이모지 (🤏 ✊ 🖐 ☝️ ✌️ 👍 ✋)
- `궤적` 켜면 검지 끝이 지나간 자취가 반투명 선으로

### 폰 송신 화면

폰에서 QR을 찍으면 열리는 화면. 세로 레이아웃.

**위쪽 (화면 절반 이상)**
- 카메라 미리보기 (검정 배경, 화면 꽉 채움)
- 좌상단 상태 배지: `상태: 대기` → `카메라 켜는 중…` → `PC에 연결 중…` → `송신 중 ✓` (오류면 빨강)

**아래쪽 컨트롤 패널**
- 라벨: "연결 코드 (PC 화면에 표시된 6자리)"
- 코드 입력칸 (가운데 정렬, 대문자, 자간 넓게 — QR로 들어오면 자동 입력)
- `카메라 켜고 연결` 버튼 (주요 스타일)
- `카메라 전환` · `연결 끊기` 버튼 (보조 스타일, 나란히)
- 안내 문구: "이 화면을 켜둔 채로 두세요. 화면이 꺼지면 영상도 멈춥니다."

---

## 색

다크 테마 하나만 써요. 영상이 주인공이라 UI는 뒤로 물러나고, 색은 정보를 구분할 때만 써요.

| 이름 | 값 | 어디에 |
|---|---|---|
| 배경 | `#0B0D10` | 페이지 바탕 |
| 패널 | `#14181D` | 카드, 폰 하단 컨트롤 영역 |
| 필드 | `#0F1317` | 입력칸, 보조 버튼, 꺼진 칩 |
| 선 | `#232A32` | 테두리, 구분선 |
| 글자 | `#E6E9EE` | 기본 텍스트 |
| 흐린 글자 | `#8A94A3` | 설명, 라벨, 로그 |
| 강조 (청록) | `#4FD1C5` | 주요 버튼, 상태 강조, 켜진 칩, **왼손** |
| 보조 강조 (주황) | `#F6AD55` | **오른손** |
| 오류 (빨강) | `#FC8181` | 오류 문구, 오류 상태 |
| 주요 버튼 글자 | `#062421` | 청록 버튼 위 텍스트 |
| 영상 바탕 | `#000000` | 카메라 영역 |

- 영상 위 배지·라벨 배경은 검정 반투명 (50~70%)
- 관절 점 테두리와 이모지는 흰색

## 글꼴

별도 웹폰트 없이 **기기 기본 글꼴**을 써요 (Windows: Segoe UI · Mac/iOS: Apple SD 고딕 Neo · 안드로이드: Noto Sans KR). 로그와 코드는 고정폭 글꼴.

| 용도 | 크기 / 굵기 |
|---|---|
| 앱 제목 | 18px / 세미볼드 |
| 카드 제목 | 12px / 세미볼드, 대문자, 자간 넓게, 흐린 색 |
| 본문 · 버튼 | 14px (폰 화면은 15px) |
| 설명 · 라벨 · 상태 줄 | 12px, 흐린 색 |
| 연결 코드 (PC) | 26px / 볼드, 자간 넓게 |
| 코드 입력칸 (폰) | 20px, 자간 넓게 |
| 라이브 상단 바 전체 | 11px |
| 로그 | 11px 고정폭 |
| 영상 위 제스처 이모지 | 28px |

숫자(fps, 신뢰도, 좌표)는 자릿수가 바뀌어도 흔들리지 않게 고정폭 숫자로 표시해요.

---

## 말투

한국어 해요체. 오류는 "무엇이 안 됐는지 + 다음에 뭘 하면 되는지"를 한 줄로. 제스처 이름은 영어 단어(Pinch, Fist, Open, Point, Peace, Thumbs) + 이모지.
