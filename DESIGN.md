# Hand Tracker — 디자인 정리

## 이 서비스는

카메라 영상을 웹 화면에 띄우고, 그 위에 보이는 **손을 실시간으로 인식해서 관절과 제스처를 그려주는** 웹앱이에요.
PC 웹캠뿐 아니라 폰 카메라를 무선으로 연결해 쓸 수 있고, 최종적으로는 스마트 글라스의 1인칭 시점 영상을 받는 것이 목표예요.
설치 없이 브라우저만으로 동작하며, 영상 분석은 전부 사용자 기기 안에서 처리돼요.

---

## 화면 구성

앱은 **두 단계**로 이루어져 있어요. 연결하기 → 보기.

### ① 연결 화면 (PC)

앱을 열면 처음 보이는 화면. 영상 소스를 하나 고르는 곳이에요.

**상단**
- **Motion Archive® 로고** (흰색 워드마크, 폭 200px) + 큰 제목 `연결하기`
- 상태 한 줄: `모델 로딩 중…` → `모델 준비 완료 · 카메라를 시작하세요` (준비되면 뒷부분이 오렌지로 강조)

**장식**
- 오른쪽 위에 큰 오렌지 점무늬 십자 모티프, 주변에 작은 시안·마젠타·회색 모티프가 흩어져 있어요 (클릭 안 됨)
- 각 카드 오른쪽 위 모서리에 작은 모티프 (로컬: 시안, 리모트: 마젠타)
- 맨 아래 푸터 한 줄: `MOTION ARCHIVE — HAND TRACKER` / `CAMERA FIRST. THE UI STEPS BACK.`

**모션 선택** (제목 아래)
- `MOTION` 라벨 + 알약 칩 `없음` / `드럼` (선택되면 오렌지), 아래에 한 줄 설명

**카드 두 장 (나란히, 좁은 화면에선 위아래)**

| 01 — LOCAL · 이 기기 카메라 | 02 — REMOTE · 폰 / 외부 기기 |
|---|---|
| 설명: "PC 웹캠으로 바로 시작해요." | 설명: "폰 카메라 영상을 받아서 트래킹해요. 글라스 연결도 이 자리에 들어와요." |
| 카메라 선택 드롭다운 | `폰 연결하기` 버튼 (보조 스타일) |
| `카메라 시작` 버튼 (주요 스타일) | 누르면 아래에 펼쳐짐 ↓ |
| | · 흰 배경 QR 코드 |
| | · 6자리 연결 코드 (크게, 자간 넓게) |
| | · 안내 문구 ("폰으로 QR을 찍거나 … 코드를 입력하세요") |
| | · "폰에서 접속할 주소" 입력칸 |
| | · 버튼 이름이 `코드 새로 만들기`로 바뀜 |

**하단**
- 최근 로그 4줄 (작은 고정폭 글씨, 오류는 빨강)

### ② 라이브 화면 (PC)

카메라가 연결되면 자동으로 넘어가요. **영상이 화면 전체를 채우고**, 그 위에 손 관절·제스처가 그려져요. 조작 요소는 상단 한 줄뿐이에요.

**상단 바** (영상 위에 얇게, 위쪽만 살짝 어둡게 처리)
- 가운데: Motion Archive 로고 (폭 96px, 살짝 투명)
- 왼쪽: `← 연결 해제` 버튼 · 소스 이름 (`PC 카메라` / `폰`) · `30 fps`
- 오른쪽:
  - 인식된 손 칩 — `L · PINCH` 처럼 손 방향 + 제스처 이름. 왼손은 시안, 오른손은 마젠타
  - 구분선
  - 토글 칩 3개: `반전` `실루엣` `궤적` (켜지면 오렌지)

**박자 바** (드럼 모션을 골랐을 때만, 상단 바 바로 아래 가운데 — 1인칭에선 손이 아래쪽에 있어서 위에 둬요)
- `드럼 · 90` 라벨 · 패턴 칸 4개 (`R L R L`, 현재 박은 손 색으로 켜지고 아래에 진행 게이지) · 최근 8박 정확도 % · `다시` `끄기`
- 카운트인 4박 동안은 칸에 `4 3 2 1`이 뜨고 정확도 자리에 `준비`
- 각 칸 위에 마지막 판정 점: 가운데 = 정확, 왼쪽 = 빠름, 오른쪽 = 늦음. 색은 정확 = 손 색, 빠름/늦음 = 오렌지, 놓침/손 바뀜 = 빨강

**틀렸을 때 알려주는 방식** — 프레임마다 바뀌는 글자는 없어요. 세 층으로만:
1. 상시: 어긋난 손의 실루엣이 채도가 빠지고 살짝 붉어져요 (천천히 이징). 맞으면 원래 색으로 돌아와요
2. 순간: 타격 지점에서 링이 퍼지며 사라져요 (0.3초). 색은 판정 색
3. 누적: 박자 바의 점과 정확도 %

**영상 위 오버레이 — 실루엣**
- 관절 점이나 뼈대 선을 그리지 않아요. 손 전체를 감싸는 **실루엣 한 덩어리**로 표시해요
- 채움은 손 색 18%, 외곽선 2px, 은은한 글로우. 손가락 두께는 손바닥 폭의 20%
- 실루엣 위 24px 지점에 제스처 이름 (`PINCH`처럼 대문자, 이모지 없음) — 검정 60% 알약 배경
- `궤적` 켜면 검지 끝이 지나간 자취가 반투명 선으로

### 폰 송신 화면

폰에서 QR을 찍으면 열리는 화면. 세로 레이아웃.

**위쪽 (화면 절반 이상)**
- 카메라 미리보기 (검정 배경, 화면 꽉 채움)
- 좌상단 상태 알약: `대기` → `카메라 켜는 중…` → `PC에 연결 중…` → `송신 중 ✓` (✓는 오렌지, 오류면 빨강 테두리)

**아래쪽 컨트롤 패널**
- 라벨: "연결 코드 (PC 화면에 표시된 6자리)"
- 코드 입력칸 (가운데 정렬, 대문자, 자간 넓게 — QR로 들어오면 자동 입력)
- `카메라 켜고 연결` 버튼 (주요 스타일)
- `카메라 전환` · `연결 끊기` 버튼 (보조 스타일, 나란히)
- 안내 문구: "이 화면을 켜둔 채로 두세요. 화면이 꺼지면 영상도 멈춥니다."

---

## 색

다크 테마 하나만 써요. 영상이 주인공이라 UI는 뒤로 물러나고, 색은 정보를 구분할 때만 써요.
시안은 왼손, 마젠타는 오른손, 오렌지는 "누르는 것", 빨강은 오류.

| 이름 | 값 | 어디에 |
|---|---|---|
| 배경 | `#0E0F12` | 페이지 바탕 |
| 패널 | `#15171C` | 카드, 폰 하단 컨트롤 영역 |
| 필드 | `#0F1116` | 입력칸, 보조 버튼, 꺼진 칩 (패널보다 한 단계 어둡게) |
| 선 | `#23262E` | 테두리, 구분선 |
| 글자 | `#F4F4F2` | 기본 텍스트, QR 바탕 |
| 보조 글자 | `#C6C9CF` | 본문 설명 |
| 흐린 글자 | `#8B8F98` | 라벨, 로그, 안내 |
| 아주 흐린 글자 | `#5C626C` | 힌트, 푸터 |
| 강조 (오렌지) | `#FFA31A` | 주요 버튼, 상태 강조, 켜진 칩, fps |
| 주요 버튼 글자 | `#1A1205` | 오렌지 버튼 위 텍스트 |
| 왼손 (시안) | `#2BD2FF` | 왼손 실루엣, 왼손 칩 |
| 오른손 (마젠타) | `#E8459B` | 오른손 실루엣, 오른손 칩 |
| 오류 (빨강) | `#FF5A5F` | 오류 문구, 오류 상태 |
| 영상 바탕 | `#000000` | 카메라 영역 |

- 영상 위 배지·라벨 배경은 검정 60%
- 손 칩 테두리는 손 색 30%

## 브랜드 에셋

`public/brand/`에 있어요. 전부 투명 배경 PNG.

| 파일 | 내용 |
|---|---|
| `logo.png` | Motion Archive® 워드마크, 흰색 |
| `motif-orange.png` | 큰 오렌지 점무늬 십자 (히어로 장식) |
| `motif-cyan.png` · `motif-magenta.png` · `motif-green.png` | 작은 점무늬 십자 (카드·배경 장식) |

## 글꼴

두 종만 써요. **Archivo**(본문·제목, 400/600/700)와 **JetBrains Mono**(라벨·로그·좌표·칩). 한글은 Apple SD 고딕 Neo / Noto Sans KR로 자동 대체돼요.
모노는 작은 라벨에만 쓰고 본문에는 쓰지 않아요.

| 용도 | 글꼴 · 크기 / 굵기 |
|---|---|
| 화면 제목 ("연결하기") | Archivo 30px / 600, 자간 좁게 |
| 카드 제목 ("이 기기 카메라") | Archivo 15px / 600 |
| 섹션 라벨 ("01 — LOCAL") | JetBrains Mono 11px, 대문자, 자간 넓게, 흐린 색 |
| 본문 · 버튼 | Archivo 14px (폰 화면은 15px) |
| 설명 · 라벨 · 상태 줄 | 12~13px, 흐린 색 |
| 연결 코드 (PC) | Archivo 30px / 600, 자간 넓게 |
| 코드 입력칸 (폰) | 18px, 자간 넓게 |
| 라이브 상단 바 전체 | JetBrains Mono 11px 대문자 |
| 로그 | JetBrains Mono 11px |
| 영상 위 제스처 라벨 | Archivo 14px / 600 대문자, 검정 60% 알약 안에 |

숫자(fps, 신뢰도, 좌표)는 자릿수가 바뀌어도 흔들리지 않게 고정폭 숫자로 표시해요.

---

## 말투

한국어 해요체. 기술 용어는 사용자 말로 감싸요(WebRTC → "폰 연결"). 오류는 "무엇이 안 됐는지 + 다음에 뭘 하면 되는지"를 한 줄로. 제스처 이름은 영어 단어(Pinch, Fist, Open, Point, Peace, Thumbs), 상세 설명은 한국어.
