# Hand Tracker — Design System

현재 `index.html`(PC 뷰어)과 `sender.html`(폰 송신)에 적용된 디자인을 정리한 문서.
두 페이지는 같은 토큰과 컴포넌트 규칙을 공유한다.

---

## 1. 디자인 방향

- **다크 UI 고정** — 카메라 영상이 주인공이라 배경은 어둡게, UI는 뒤로 물러난다. 라이트 테마 없음.
- **도구(Tool)의 느낌** — 장식 없이 상태·수치·컨트롤만. 개발자 도구/카메라 앱 계열.
- **컬러는 정보용으로만** — 청록(왼손/정상), 주황(오른손), 빨강(오류). 브랜드 컬러가 아니라 데이터 구분용.

---

## 2. 컬러 토큰

CSS 변수로 `:root`에 정의. 두 파일 동일.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#0b0d10` | 페이지 배경 |
| `--panel` | `#14181d` | 사이드 패널, 하단 컨트롤 영역 |
| `--line` | `#232a32` | 테두리, 구분선 |
| `--text` | `#e6e9ee` | 기본 텍스트 |
| `--muted` | `#8a94a3` | 보조 텍스트, 라벨, 로그 |
| `--accent` | `#4fd1c5` | 주요 버튼, 강조 텍스트, **왼손** 오버레이 |
| `--accent-2` | `#f6ad55` | **오른손** 오버레이 |
| `--danger` | `#fc8181` | 오류 메시지, 오류 상태 |

토큰 외 고정값:

| 값 | 용도 |
|---|---|
| `#000` | 영상 스테이지 배경 |
| `#0f1317` | 입력 필드, 보조 버튼, 손 카드 배경 (패널보다 한 단계 더 어두움) |
| `#062421` | 주요 버튼 위 텍스트 (accent 위 대비용 딥 그린) |
| `#fff` | 관절 점 테두리, 제스처 이모지 |
| `rgba(0,0,0,.55~.6)` | 영상 위 라벨/배지 배경 |

### 손 구분 컬러
- 왼손 → `--accent` 청록
- 오른손 → `--accent-2` 주황
- 손 카드 테두리는 해당 컬러에 알파 25% (`#4fd1c540` 형태)

---

## 3. 타이포그래피

```css
font: 14px/1.5 system-ui, -apple-system, "Segoe UI", "Apple SD Gothic Neo", "Noto Sans KR", sans-serif;
```
시스템 폰트만 사용. 웹폰트 없음 (로컬/오프라인 동작 우선). 폰 페이지는 터치 가독성 위해 기본 15px.

| 역할 | 크기 / 굵기 | 비고 |
|---|---|---|
| 페이지 제목 (`h1`) | 18px / 600 | `letter-spacing: -0.01em` |
| 섹션 제목 (`h2`) | 12px / 600 | 대문자, `letter-spacing: 0.08em`, muted 컬러 |
| 본문 | 14px / 400 | |
| 라벨 | 12px | muted |
| 상태 텍스트 (헤더) | 12px | muted, 강조 부분만 `<b>` accent |
| 제스처 이름 (손 카드) | 22px / 700 | `letter-spacing: -0.02em` |
| 페어링 코드 | 26px / 700 | `letter-spacing: 0.15em` |
| 코드 입력 (폰) | 20px | `letter-spacing: 0.2em`, 대문자, 중앙 정렬 |
| 로그 | 11px | 모노스페이스 (`ui-monospace, Menlo, Consolas`) |
| 영상 위 제스처 이모지 | 28px / bold | 그림자 대신 검정 오프셋 1px 한 겹 |

숫자는 `font-variant-numeric: tabular-nums` (신뢰도, 좌표, fps).

---

## 4. 레이아웃 — 2단계

### ① 연결 화면 (`ConnectScreen`)
```
              Hand Tracker
        모델 준비 완료 · 카메라를 시작하세요

┌ 이 기기 카메라 ───────┐ ┌ 폰 / 외부 기기 ──────┐
│ PC 웹캠으로 바로 시작   │ │ 폰 카메라 영상을 받아서  │
│ [카메라 ▾]            │ │ [폰 연결하기]          │
│                      │ │  (QR / 코드 / 주소)    │
│ [카메라 시작]          │ │                      │
└──────────────────────┘ └──────────────────────┘
        [최근 로그 4줄, 11px mono]
```
- 세로 중앙 정렬 아님 — 상단에서 `py-8`, 콘텐츠 `max-width: 640px`
- 카드 2열 (`sm` 이상), 좁으면 1열. 카드는 `.panel`
- 로그는 최근 4줄만, 카드 아래

### ② 라이브 화면 (`Stage fill` + `LiveBar`)
```
┌──────────────────────────────────────────────────────┐
│ [← 연결 해제] 폰 · 30 fps        [L 🤏 Pinch] │ 반전 관절 궤적 │ ← LiveBar
│                                                      │
│                                                      │
│                   영상 (화면 전체, contain)              │
│                                                      │
│                                                      │
└──────────────────────────────────────────────────────┘
```
- `position: fixed; inset: 0`, 배경 검정, 영상 `object-fit: contain`
- **LiveBar**: 상단 고정, 높이 최소. 위에서 아래로 `black/70 → transparent` 그라디언트, `px-3 pt-2 pb-6`
  - 왼쪽: 연결 해제 버튼 + 소스명 · fps
  - 오른쪽: 손 칩(측 이니셜 + 이모지 + 제스처, 손 컬러 테두리) → 구분선 → 토글 칩 3개
  - 모든 요소 11px, `leading-none`, radius 6px. 크게 만들지 않는다 — 영상이 주인공
- 사이드 패널 없음. 상세 수치(신뢰도, 좌표)는 손 칩의 `title` 툴팁으로만

### 폰 송신 (`Sender`)
```
┌─────────────────┐
│ ● 상태: 송신 중  │ ← 배지 (좌상단 고정)
│   카메라 프리뷰   │ flex: 1, object-fit: cover
├─────────────────┤
│ 연결 코드 입력    │
│ [카메라 켜고 연결] │ 하단 컨트롤 (panel)
│ [전환] [끊기]    │
└─────────────────┘
```
- 하단 패딩에 `env(safe-area-inset-bottom)`, `viewport-fit=cover`

## 5. 컴포넌트

### 패널 (`.panel`)
- 배경 `--panel`, 테두리 `--line`, radius 12px, 패딩 14px
- 내부 섹션 간격 14px (`flex-direction: column; gap: 14px`)
- 각 섹션은 `h2` 제목으로 시작

### 버튼
| 종류 | 스타일 |
|---|---|
| 주요 (`button`) | 배경 `--accent`, 텍스트 `#062421`, 600 굵기, 테두리 없음 |
| 보조 (`.secondary`) | 배경 `#0f1317`, 텍스트 `--text`, 테두리 `--line`, 500 굵기 |
| 비활성 | `opacity: .5`, `cursor: not-allowed` |

- 패딩 8px 10px (PC) / 12px (폰), radius 8px (PC) / 10px (폰)
- 항상 `width: 100%`. 나란히 놓을 땐 `.row` (flex, gap 8px)

### 입력 (`select`, `input`)
- 배경 `#0f1317`, 테두리 `--line`, radius 8px, 텍스트 `--text`
- 위에 `label` 12px muted

### 토글 (`.toggle`)
- 네이티브 체크박스 + 텍스트, `accent-color: var(--accent)`
- 13px, gap 8px

### 칩 (LiveBar 전용)
- 토글 칩: 11px, `px-2 py-1`, radius 6px, 테두리 `--line` / 배경 `#0f1317` / 텍스트 muted → 켜짐: 테두리 accent 60%, 배경 accent 15%, 텍스트 accent. `aria-pressed`
- 손 칩: 같은 크기, 배경 `black/50`, 테두리·텍스트 손 컬러(테두리 50%)
- 해제 버튼: 토글 칩과 같은 크기, hover 시 danger

### 손 카드 (`.hand`) — ①에서는 안 씀, 향후 상세 패널용
```
┌───────────────────────────┐ ← 테두리: 손 컬러 25%
│ 왼손            신뢰도 97% │
│ 🤏 Pinch                  │ 22px bold
│ 핀치 12% · 검지 (0.43, 0.61) │ 12px muted
└───────────────────────────┘
```
- 배경 `#0f1317`, radius 10px, 패딩 10px, 카드 간격 8px
- 없을 때 `.empty` — "아직 없음" muted 13px

### 상태 배지 (폰, `.badge`)
- 좌상단 12px 오프셋, `rgba(0,0,0,.6)` 배경, pill (`border-radius: 999px`), 12px
- 값 부분 `<b>` accent / 오류 시 `.err` → danger

### 페어링 박스 (`#pairBox`)
- QR: 160×160, 흰 배경 + 8px 패딩 + radius 10px (스캔 대비 확보)
- 아래 6자리 코드 26px, 힌트 12px muted, 접속 주소 입력 필드
- localhost 주소일 땐 힌트를 `.err`로 경고

### 로그 (`.log`)
- 최대 높이 140px 스크롤, 최신이 위 (`prepend`)
- `[HH:MM:SS AM] 메시지` 형식, 오류 라인 `.err`

### 플레이스홀더 (`.placeholder`)
- 스테이지 전체 덮는 `display: grid; place-items: center`, muted 텍스트 중앙 정렬
- 안내 문구 + `<small>` + 시작 버튼

---

## 6. 영상 오버레이 (Canvas)

| 요소 | 스타일 |
|---|---|
| 관절 연결선 | 손 컬러, `lineWidth: 3` |
| 관절 점 | 채움 손 컬러, 테두리 `#fff` 1px, 반지름 4 |
| 검지 궤적 | 손 컬러, `lineWidth: 3`, `lineCap: round`, alpha 0.7, 최근 40프레임 |
| 제스처 이모지 | 검지 끝 위 24px 지점, 28px bold, 검정 → 흰색 1px 오프셋 |

- 미러 모드일 때 비디오는 `scaleX(-1)`, 캔버스도 같은 변환 → 이모지 텍스트만 정방향으로 별도 그리기
- 원격(폰) 영상은 미러 자동 해제

---

## 7. 상태 & 피드백

| 상태 | 표시 위치 | 문구 예 |
|---|---|---|
| 모델 로딩 | 헤더 | 모델 로딩 중… |
| 준비 | 헤더 | 모델 준비 완료 · **카메라를 시작하세요** |
| 추적 중 | 헤더 | 추적 중 · **30 fps** · 손 1개 |
| 원격 연결 | 헤더 | 원격 기기 연결됨 · 추적 시작 |
| 페어링 대기 | 로그 | 페어링 대기: 코드 B93U4V |
| 오류 | 헤더 + 로그 `.err` | 카메라 접근 실패 |
| 폰 대기 / 연결 중 / 송신 중 | 배지 | 대기 → 카메라 켜는 중… → PC에 연결 중… → 송신 중 ✓ |
| 폰 오류 | 배지 `.err` | PC에서 '폰 연결'을 먼저 눌러주세요 |

원칙: **헤더 한 줄 = 현재 상태**, **로그 = 히스토리**. 모달/토스트 없음.

---

## 8. 카피 톤

- 한국어, 해요체 ("허용해 주세요", "누르세요")
- 기술 용어는 그대로 (fps, WebRTC 대신 "폰 연결"처럼 사용자 언어로 감싸되, 로그엔 원어)
- 오류는 원인 + 다음 행동 한 줄 ("localhost 주소는 폰에서 열 수 없어요. … IP를 넣으세요")
- 제스처 이름은 영어 단어 (Pinch, Fist, Open, Point, Peace, Thumbs) + 이모지, 상세는 한국어

---

## 9. 구현 위치

- 토큰: `app/globals.css`의 `@theme` 블록 → Tailwind 유틸(`bg-bg`, `text-muted`, `border-line`, `bg-accent`, `text-danger` …)
- 컴포넌트 클래스: 같은 파일 `@layer components` — `.panel`, `.section-title`, `.label`, `.field`, `.btn`, `.btn-secondary`, `.toggle`, `.err`
- 오버레이 수치(선 굵기, 점 반지름, 궤적 길이, 손 컬러): `data/settings.json`
- 제스처 이름/이모지: `data/gestures.json`

새 색이나 간격이 필요하면 컴포넌트에 hex를 직접 쓰지 말고 `@theme`에 토큰을 추가한 뒤 이 문서 §2에 반영한다.
